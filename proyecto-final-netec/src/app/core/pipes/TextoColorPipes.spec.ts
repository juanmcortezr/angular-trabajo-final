import { TextoColorPipe } from "./TextoColorPipe";

describe('TextoColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoColorPipe();
    expect(pipe).toBeTruthy();
  });
});
